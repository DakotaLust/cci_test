version: 2.1

jobs:
  my-job:
    docker:
      - image: cimg/node:20.5.1
    steps:
      - checkout
      - run: npm install
      - run: cat package.json
      - run: pwd && ls
      - run: set -e
      - run: npm run test:ci --maxsockets 1

workflows:
  my-workflow:
    jobs:
      - my-job
