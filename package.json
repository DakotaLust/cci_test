{
  "devDependencies": {
    "jest": "^27.5.1"
  },
  "scripts": {
    "test": "NODE_ENV=test jest --verbose --runInBand --coverage --clearMocks --detectOpenHandles --forceExit && jest --coverage && true"
  }
}
