version: 2.1

parameters:
  falsy-param:
    type: boolean
    default: false

jobs:
  my-job:
    docker:
      - image: cimg/base:2022.03
    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
  my-workflow:
    jobs:
      - my-job
