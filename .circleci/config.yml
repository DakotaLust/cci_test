version: 2.1

jobs:
  my-job:
    docker:
      - image: cimg/node:20.5.1
    steps:
      - checkout
      - run: npm install
      - run: cat package.json
      - run: pwd && ls
      - run: npm run test:ci

workflows:
  my-workflow:
    jobs:
      - my-job
